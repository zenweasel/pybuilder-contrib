============
Installation
============

At the command line::

    $ easy_install pybuilder-contrib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pybuilder-contrib
    $ pip install pybuilder-contrib